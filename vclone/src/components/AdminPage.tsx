import { Typography, Box } from "@mui/material";

export const AdminPage = () => {
    return (
        <Box>
            <Typography>Admin Dashboard</Typography>
            <p>Welcome Admin</p>
        </Box>
    )
}